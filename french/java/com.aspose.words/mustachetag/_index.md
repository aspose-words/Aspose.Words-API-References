---
title: "MustacheTag"
linktitle: "MustacheTag"
second_title: "Aspose.Words pour Java"
description: "Représente une balise mustache en Java."
type: docs
weight: 474
url: /fr/java/com.aspose.words/mustachetag/
---

**Inheritance:**
java.lang.Object
```
public class MustacheTag
```

Représente la balise "mustache".
## Méthodes

| Méthode | Description |
| --- | --- |
| [getReferenceOffset()](#getReferenceOffset) | Obtient la position de départ basée sur zéro de la balise à partir du début du [getReferenceRun()](../../com.aspose.words/mustachetag/\#getReferenceRun). |
| [getReferenceRun()](#getReferenceRun) | Obtient le run qui contient le début de la balise. |
| [getText()](#getText) | Obtient le texte de la balise. |
### getReferenceOffset() {#getReferenceOffset}
```
public int getReferenceOffset()
```


Obtient la position de départ basée sur zéro de la balise à partir du début du [getReferenceRun()](../../com.aspose.words/mustachetag/\#getReferenceRun).

**Returns:**
int - La position de départ basée sur zéro de la balise à partir du début du [getReferenceRun()](../../com.aspose.words/mustachetag/\#getReferenceRun).
### getReferenceRun() {#getReferenceRun}
```
public Run getReferenceRun()
```


Obtient le run qui contient le début de la balise.

**Returns:**
[Run](../../com.aspose.words/run/) - The run that contains the beginning of the tag.
### getText() {#getText}
```
public String getText()
```


Obtient le texte de la balise.

**Returns:**
java.lang.String - Le texte de la balise.
