---
title: Class Merger
second_title: Référence de l'API Aspose.Words pour .NET
description: Aspose.Words.LowCode.Merger classe. Représente un groupe de méthodes destinées à fusionner différents types de documents en un seul document de sortie.
type: docs
weight: 3760
url: /fr/net/aspose.words.lowcode/merger/
---
## Merger class

Représente un groupe de méthodes destinées à fusionner différents types de documents en un seul document de sortie.

```csharp
public static class Merger
```

## Méthodes

| Nom | La description |
| --- | --- |
| static [Merge](../../aspose.words.lowcode/merger/merge/#merge)(Stream[], MergeFormatMode) | Fusionne les documents d'entrée donnés en un seul document et renvoie[`Document`](../../aspose.words/document/) instance du document final. |
| static [Merge](../../aspose.words.lowcode/merger/merge/#merge_4)(string, string[]) | Fusionne les documents d'entrée donnés en un seul document de sortie en utilisant les noms de fichiers d'entrée et de sortie spécifiés. |
| static [Merge](../../aspose.words.lowcode/merger/merge/#merge_1)(string[], MergeFormatMode) | Fusionne les documents d'entrée donnés en un seul document et renvoie[`Document`](../../aspose.words/document/) instance du document final. |
| static [Merge](../../aspose.words.lowcode/merger/merge/#merge_2)(Stream, Stream[], SaveFormat) | Fusionne les documents d'entrée donnés en un seul document de sortie en utilisant les flux d'entrée-sortie spécifiés et le format du document final. |
| static [Merge](../../aspose.words.lowcode/merger/merge/#merge_3)(Stream, Stream[], SaveOptions, MergeFormatMode) | Fusionne les documents d'entrée donnés en un seul document de sortie en utilisant les flux d'entrée-sortie spécifiés et les options d'enregistrement. |
| static [Merge](../../aspose.words.lowcode/merger/merge/#merge_5)(string, string[], SaveFormat, MergeFormatMode) | Fusionne les documents d'entrée donnés en un seul document de sortie en utilisant les noms de fichiers d'entrée-sortie spécifiés et le format du document final. |
| static [Merge](../../aspose.words.lowcode/merger/merge/#merge_6)(string, string[], SaveOptions, MergeFormatMode) | Fusionne les documents d'entrée donnés en un seul document de sortie en utilisant les noms de fichiers d'entrée-sortie spécifiés et les options d'enregistrement. |

### Remarques

Les fichiers ou flux d'entrée et de sortie spécifiés, ainsi que les options de fusion et d'enregistrement souhaitées, , sont utilisés pour fusionner les documents d'entrée donnés en un seul document de sortie.

La fonctionnalité de fusion prend en charge plus de 35 formats de fichiers différents.

### Voir également

* espace de noms [Aspose.Words.LowCode](../../aspose.words.lowcode/)
* Assemblée [Aspose.Words](../../)


