---
title: "RefInt"
linktitle: "RefInt"
second_title: "Aspose.Words per Java"
description: "Rappresenta le costruzioni Out/Ref per il tipo primitivo Integer in Java."
type: docs
weight: 15
url: /it/java/com.aspose.words.ref/refint/
---

**Inheritance:**
java.lang.Object
```
public class RefInt
```

Rappresenta costrutti Out/Ref per il tipo primitivo Integer
## Costruttori

| Costruttore | Descrizione |
| --- | --- |
| [RefInt(int value)](#RefInt-int) | Mima l'inizializzazione del parametro Out/Ref con il valore fornito. |
| [RefInt(long value)](#RefInt-long) | Mima l'inizializzazione del parametro Out/Ref con il valore fornito. |
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [get()](#get) | Ottiene il valore del parametro Out/Ref. Deve essere usato dopo aver chiamato il metodo con le costruzioni Out/Ref |
| [set(int anotherValue)](#set-int) | Imposta il valore del parametro Out/Ref. Deve essere usato all'interno del metodo con le costruzioni Out/Ref |
| [set(long anotherValue)](#set-long) | Imposta il valore del parametro Out/Ref. Deve essere usato all'interno del metodo con le costruzioni Out/Ref |
| [toString()](#toString) |  |
### RefInt(int value) {#RefInt-int}
```
public RefInt(int value)
```


Mima l'inizializzazione del parametro Out/Ref con il valore fornito. Deve essere usato prima di chiamare il metodo con le costruzioni Out/Ref

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore | int |  |

### RefInt(long value) {#RefInt-long}
```
public RefInt(long value)
```


Mima l'inizializzazione del parametro Out/Ref con il valore fornito. Deve essere usato prima di chiamare il metodo con le costruzioni Out/Ref

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore | long |  |

### get() {#get}
```
public int get()
```


Ottiene il valore del parametro Out/Ref. Deve essere usato dopo aver chiamato il metodo con le costruzioni Out/Ref

**Returns:**
int
### set(int anotherValue) {#set-int}
```
public int set(int anotherValue)
```


Imposta il valore del parametro Out/Ref. Deve essere usato all'interno del metodo con le costruzioni Out/Ref

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| anotherValue | int |  |

**Returns:**
int
### set(long anotherValue) {#set-long}
```
public int set(long anotherValue)
```


Imposta il valore del parametro Out/Ref. Deve essere usato all'interno del metodo con le costruzioni Out/Ref

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| anotherValue | long |  |

**Returns:**
int
### toString() {#toString}
```
public String toString()
```




**Returns:**
java.lang.String
