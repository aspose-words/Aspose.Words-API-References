---
title: "XmlDataLoadOptions"
linktitle: "XmlDataLoadOptions"
second_title: "Aspose.Words لـ Java"
description: "يمثل خيارات تحميل بيانات XML في جافا."
type: docs
weight: 745
url: /ar/java/com.aspose.words/xmldataloadoptions/
---

**Inheritance:**
java.lang.Object
```
public class XmlDataLoadOptions
```

يمثل خيارات تحميل بيانات XML.

للتعرف على المزيد، زر مقالة توثيق [ LINQ Reporting Engine ][LINQ Reporting Engine].

 **Remarks:** 

يمكن تمرير نسخة من هذه الفئة إلى مُنشئات [XmlDataSource](../../com.aspose.words/xmldatasource/).


[LINQ Reporting Engine]: https://docs.aspose.com/words/java/linq-reporting-engine/
## المنشئات

| المنشئ | الوصف |
| --- | --- |
| [XmlDataLoadOptions()](#XmlDataLoadOptions) | يُنشئ نسخة جديدة من هذه الفئة باستخدام الخيارات الافتراضية. |
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getAlwaysGenerateRootObject()](#getAlwaysGenerateRootObject) | يحصل على علامة تشير إلى ما إذا كان مصدر البيانات المُولد سيحتوي دائمًا على كائن لعناصر الجذر XML. |
| [setAlwaysGenerateRootObject(boolean value)](#setAlwaysGenerateRootObject-boolean) | يضبط علامة تشير إلى ما إذا كان مصدر البيانات المُولد سيحتوي دائمًا على كائن لعناصر الجذر XML. |
### XmlDataLoadOptions() {#XmlDataLoadOptions}
```
public XmlDataLoadOptions()
```


يُنشئ نسخة جديدة من هذه الفئة باستخدام الخيارات الافتراضية.

### getAlwaysGenerateRootObject() {#getAlwaysGenerateRootObject}
```
public boolean getAlwaysGenerateRootObject()
```


يحصل على علامة تشير إلى ما إذا كان مصدر البيانات المُولد سيحتوي دائمًا على كائن لعناصر الجذر XML. إذا كان عنصر الجذر XML لا يحتوي على سمات وجميع عناصره الفرعية لها نفس الأسماء، فلن يتم إنشاء مثل هذا الكائن بشكل افتراضي.

 **Remarks:** 

القيمة الافتراضية هي false.

**Returns:**
boolean - علامة تشير إلى ما إذا كان مصدر البيانات المُولد سيحتوي دائمًا على كائن لعناصر الجذر XML.
### setAlwaysGenerateRootObject(boolean value) {#setAlwaysGenerateRootObject-boolean}
```
public void setAlwaysGenerateRootObject(boolean value)
```


يضبط علامة تشير إلى ما إذا كان مصدر البيانات المُولد سيحتوي دائمًا على كائن لعناصر الجذر XML. إذا كان عنصر الجذر XML لا يحتوي على سمات وجميع عناصره الفرعية لها نفس الأسماء، فلن يتم إنشاء مثل هذا الكائن بشكل افتراضي.

 **Remarks:** 

القيمة الافتراضية هي false.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة | boolean | علامة تشير إلى ما إذا كان مصدر البيانات المُولد سيحتوي دائمًا على كائن لعناصر الجذر XML. |

