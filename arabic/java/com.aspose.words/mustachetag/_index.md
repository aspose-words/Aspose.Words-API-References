---
title: "MustacheTag"
linktitle: "MustacheTag"
second_title: "Aspose.Words لـ Java"
description: "يمثل علامة mustache في Java."
type: docs
weight: 474
url: /ar/java/com.aspose.words/mustachetag/
---

**Inheritance:**
java.lang.Object
```
public class MustacheTag
```

يمثل وسم \"mustache\".
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getReferenceOffset()](#getReferenceOffset) | يحصل على الموضع الابتدائي الصفري للعلامة من بداية [getReferenceRun()](../../com.aspose.words/mustachetag/\#getReferenceRun). |
| [getReferenceRun()](#getReferenceRun) | يحصل على المقطع الذي يحتوي على بداية العلامة. |
| [getText()](#getText) | يحصل على نص العلامة. |
### getReferenceOffset() {#getReferenceOffset}
```
public int getReferenceOffset()
```


يحصل على الموضع الابتدائي الصفري للعلامة من بداية [getReferenceRun()](../../com.aspose.words/mustachetag/\#getReferenceRun).

**Returns:**
int - الموضع الابتدائي الصفري للعلامة من بداية [getReferenceRun()](../../com.aspose.words/mustachetag/\#getReferenceRun).
### getReferenceRun() {#getReferenceRun}
```
public Run getReferenceRun()
```


يحصل على المقطع الذي يحتوي على بداية العلامة.

**Returns:**
[Run](../../com.aspose.words/run/) - The run that contains the beginning of the tag.
### getText() {#getText}
```
public String getText()
```


يحصل على نص العلامة.

**Returns:**
java.lang.String - نص العلامة.
