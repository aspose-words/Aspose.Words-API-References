---
title: "ChartAxisTitle"
linktitle: "ChartAxisTitle"
second_title: "Aspose.Words for Java"
description: "提供对 Java 中轴标题属性的访问。"
type: docs
weight: 69
url: /zh/java/com.aspose.words/chartaxistitle/
---

**Inheritance:**
java.lang.Object
```
public class ChartAxisTitle
```

提供对轴标题属性的访问。

欲了解更多，请访问 [ Working with Charts ][Working with Charts] 文档文章。

 **Examples:** 

展示如何设置图表轴标题。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```


[Working with Charts]: https://docs.aspose.com/words/java/working-with-charts/
## 方法

| 方法 | 描述 |
| --- | --- |
| [getFont()](#getFont) | 提供对轴标题字体格式的访问。 |
| [getFormat()](#getFormat) | 提供对轴标题填充和线条格式的访问。 |
| [getOrientation()](#getOrientation) | 获取轴标题文本的方向。 |
| [getOverlay()](#getOverlay) | 确定是否允许其他图表元素覆盖标题。 |
| [getRotation()](#getRotation) | 获取轴标题的旋转角度（度）。 |
| [getShow()](#getShow) | 确定是否在轴上显示标题。 |
| [getText()](#getText) | 获取轴标题的文本。 |
| [setOrientation(int value)](#setOrientation-int) | 设置轴标题文本的方向。 |
| [setOverlay(boolean value)](#setOverlay-boolean) | 确定是否允许其他图表元素覆盖标题。 |
| [setRotation(int value)](#setRotation-int) | 设置轴标题的旋转角度（度）。 |
| [setShow(boolean value)](#setShow-boolean) | 确定是否在轴上显示标题。 |
| [setText(String value)](#setText-java.lang.String) | 设置轴标题的文本。 |
### getFont() {#getFont}
```
public Font getFont()
```


提供对轴标题字体格式的访问。

 **Examples:** 

展示如何设置图表轴标题。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Returns:**
[Font](../../com.aspose.words/font/) - The corresponding [Font](../../com.aspose.words/font/) value.
### getFormat() {#getFormat}
```
public ChartFormat getFormat()
```


提供对轴标题填充和线条格式的访问。

 **Examples:** 

展示如何使用图表格式化。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);
 Chart chart = shape.getChart();

 // Delete series generated by default.
 ChartSeriesCollection series = chart.getSeries();
 series.clear();

 String[] categories = new String[] { "Category 1", "Category 2" };
 series.add("Series 1", categories, new double[] { 1.0, 2.0 });
 series.add("Series 2", categories, new double[] { 3.0, 4.0 });

 // Format chart background.
 chart.getFormat().getFill().solid(Color.darkGray);

 // Hide axis tick labels.
 chart.getAxisX().getTickLabels().setPosition(AxisTickLabelPosition.NONE);
 chart.getAxisY().getTickLabels().setPosition(AxisTickLabelPosition.NONE);

 // Format chart title.
 chart.getTitle().getFormat().getFill().solid(Color.yellow);

 // Format axis title.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().getFormat().getFill().solid(Color.yellow);

 // Format legend.
 chart.getLegend().getFormat().getFill().solid(Color.yellow);

 doc.save(getArtifactsDir() + "Charts.ChartFormat.docx");
 
```

**Returns:**
[ChartFormat](../../com.aspose.words/chartformat/) - The corresponding [ChartFormat](../../com.aspose.words/chartformat/) value.
### getOrientation() {#getOrientation}
```
public int getOrientation()
```


获取轴标题文本的方向。

 **Remarks:** 

默认值为 [ShapeTextOrientation.HORIZONTAL](../../com.aspose.words/shapetextorientation/\#HORIZONTAL)。

 **Examples:** 

展示如何设置图表和轴标题的方向和旋转。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);
 Shape chartShape = builder.insertChart(ChartType.COLUMN, 400.0, 300.0);
 Chart chart = chartShape.getChart();

 chart.getTitle().setText("Sample Chart");
 chart.getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getTitle().setRotation(90);

 // Before setting title properties, make sure that this title will be displayed.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().setText("X Axis");
 chart.getAxisX().getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getAxisX().getTitle().setRotation(-90);

 doc.save(getArtifactsDir() + "Charts.TitleOrientation.docx");
 
```

**Returns:**
int - 轴标题文本的方向。返回值是 [ShapeTextOrientation](../../com.aspose.words/shapetextorientation/) 常量之一。
### getOverlay() {#getOverlay}
```
public boolean getOverlay()
```


确定是否允许其他图表元素覆盖标题。默认值为 false。

 **Examples:** 

展示如何设置图表轴标题。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Returns:**
boolean - 相应的 boolean 值。
### getRotation() {#getRotation}
```
public int getRotation()
```


获取轴标题的旋转角度（度）。

 **Remarks:** 

可接受值的范围为 -180 到 180（含）。

 **Examples:** 

展示如何设置图表和轴标题的方向和旋转。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);
 Shape chartShape = builder.insertChart(ChartType.COLUMN, 400.0, 300.0);
 Chart chart = chartShape.getChart();

 chart.getTitle().setText("Sample Chart");
 chart.getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getTitle().setRotation(90);

 // Before setting title properties, make sure that this title will be displayed.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().setText("X Axis");
 chart.getAxisX().getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getAxisX().getTitle().setRotation(-90);

 doc.save(getArtifactsDir() + "Charts.TitleOrientation.docx");
 
```

**Returns:**
int - 轴标题的旋转角度（度）。
### getShow() {#getShow}
```
public boolean getShow()
```


确定是否应在坐标轴上显示标题。默认值为 false。

 **Examples:** 

展示如何设置图表轴标题。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Returns:**
boolean - 相应的 boolean 值。
### getText() {#getText}
```
public String getText()
```


获取坐标轴标题的文本。如果指定了 null 或空值，将显示自动生成的标题。

 **Remarks:** 

如果需要显示标题，请使用 [getShow()](../../com.aspose.words/chartaxistitle/\#getShow) / [setShow(boolean)](../../com.aspose.words/chartaxistitle/\#setShow-boolean) 选项。

 **Examples:** 

展示如何设置图表轴标题。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Returns:**
java.lang.String - 坐标轴标题的文本。
### setOrientation(int value) {#setOrientation-int}
```
public void setOrientation(int value)
```


设置轴标题文本的方向。

 **Remarks:** 

默认值为 [ShapeTextOrientation.HORIZONTAL](../../com.aspose.words/shapetextorientation/\#HORIZONTAL)。

 **Examples:** 

展示如何设置图表和轴标题的方向和旋转。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);
 Shape chartShape = builder.insertChart(ChartType.COLUMN, 400.0, 300.0);
 Chart chart = chartShape.getChart();

 chart.getTitle().setText("Sample Chart");
 chart.getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getTitle().setRotation(90);

 // Before setting title properties, make sure that this title will be displayed.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().setText("X Axis");
 chart.getAxisX().getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getAxisX().getTitle().setRotation(-90);

 doc.save(getArtifactsDir() + "Charts.TitleOrientation.docx");
 
```

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | int | 坐标轴标题文本的方向。该值必须是 [ShapeTextOrientation](../../com.aspose.words/shapetextorientation/) 常量之一。 |

### setOverlay(boolean value) {#setOverlay-boolean}
```
public void setOverlay(boolean value)
```


确定是否允许其他图表元素覆盖标题。默认值为 false。

 **Examples:** 

展示如何设置图表轴标题。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | boolean | 相应的 boolean 值。 |

### setRotation(int value) {#setRotation-int}
```
public void setRotation(int value)
```


设置轴标题的旋转角度（度）。

 **Remarks:** 

可接受值的范围为 -180 到 180（含）。

 **Examples:** 

展示如何设置图表和轴标题的方向和旋转。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);
 Shape chartShape = builder.insertChart(ChartType.COLUMN, 400.0, 300.0);
 Chart chart = chartShape.getChart();

 chart.getTitle().setText("Sample Chart");
 chart.getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getTitle().setRotation(90);

 // Before setting title properties, make sure that this title will be displayed.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().setText("X Axis");
 chart.getAxisX().getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getAxisX().getTitle().setRotation(-90);

 doc.save(getArtifactsDir() + "Charts.TitleOrientation.docx");
 
```

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | int | 坐标轴标题的旋转角度（以度为单位）。 |

### setShow(boolean value) {#setShow-boolean}
```
public void setShow(boolean value)
```


确定是否应在坐标轴上显示标题。默认值为 false。

 **Examples:** 

展示如何设置图表轴标题。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | boolean | 相应的 boolean 值。 |

### setText(String value) {#setText-java.lang.String}
```
public void setText(String value)
```


设置坐标轴标题的文本。如果指定了 null 或空值，将显示自动生成的标题。

 **Remarks:** 

如果需要显示标题，请使用 [getShow()](../../com.aspose.words/chartaxistitle/\#getShow) / [setShow(boolean)](../../com.aspose.words/chartaxistitle/\#setShow-boolean) 选项。

 **Examples:** 

展示如何设置图表轴标题。

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | java.lang.String | 坐标轴标题的文本。 |

