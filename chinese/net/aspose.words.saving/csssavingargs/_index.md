---
title: CssSavingArgs
second_title: Aspose.Words for .NET API 参考
description: 为CssSaving./icsssavingcallback/csssaving事件提供数据
type: docs
weight: 4570
url: /zh/net/aspose.words.saving/csssavingargs/
---
## CssSavingArgs class

为[`CssSaving`](../icsssavingcallback/csssaving)事件提供数据。

```csharp
public class CssSavingArgs
```

## 特性

| 姓名 | 描述 |
| --- | --- |
| [CssStream](../../aspose.words.saving/csssavingargs/cssstream) { get; set; } | 允许指定 CSS 信息将保存到的流。 |
| [Document](../../aspose.words.saving/csssavingargs/document) { get; } | 获取当前正在保存的文档对象。 |
| [IsExportNeeded](../../aspose.words.saving/csssavingargs/isexportneeded) { get; set; } | 允许指定是否将 CSS 导出到文件并嵌入到 HTML 文档中。默认为` true` 。 当此属性为` false` 时，CSS 信息不会保存到 CSS 文件中，也不会嵌入到 HTML 文档中。 |
| [KeepCssStreamOpen](../../aspose.words.saving/csssavingargs/keepcssstreamopen) { get; set; } | 指定 Aspose.Words 应该在保存 CSS 信息后保持流打开还是关闭它。 |

### 评论

默认情况下，当 Aspose.Words 将文档保存为 HTML 时，它会内联保存 CSS 信息 （作为 **样式** 每个元素的属性的值）。

[`CssSavingArgs`](../csssavingargs)允许通过提供您的自己的流对象。

要将 CSS 保存到流中，请使用[`CssStream`](./cssstream)属性。

要禁止将 CSS 保存到文件中并嵌入到 HTML 文档中，请使用[`IsExportNeeded`](./isexportneeded)属性.

### 例子

展示如何使用 HTML 转换创建的 CSS 样式表。

```csharp
public void ExternalCssFilenames()
{
    Document doc = new Document(MyDir + "Rendering.docx");

     // 创建一个“HtmlFixedSaveOptions”对象，我们可以将它传递给文档的“Save”方法
     // 修改我们如何将文档转换为 HTML.
    HtmlSaveOptions options = new HtmlSaveOptions();

     // 将“CssStylesheetType”属性设置为“CssStyleSheetType.External” to
     // 将保存的 HTML 文档与外部 CSS 样式表文件一起保存。
    options.CssStyleSheetType = CssStyleSheetType.External;

     // 下面是为输出 CSS 样式表指定目录和文件名的两种方式。
    // 1 - 使用“CssStyleSheetFileName”属性为我们的样式表分配一个文件名：
    options.CssStyleSheetFileName = ArtifactsDir + "SavingCallback.ExternalCssFilenames.css";

     // 2 - 使用自定义回调来命名我们的样式表：
    options.CssSavingCallback =
        new CustomCssSavingCallback(ArtifactsDir + "SavingCallback.ExternalCssFilenames.css", true, false);

    doc.Save(ArtifactsDir + "SavingCallback.ExternalCssFilenames.html", options);
}

/// <summary>
 /// 设置自定义文件名，以及外部 CSS 样式表的其他参数。
/// </summary>
private class CustomCssSavingCallback : ICssSavingCallback
{
    public CustomCssSavingCallback(string cssDocFilename, bool isExportNeeded, bool keepCssStreamOpen)
    {
        mCssTextFileName = cssDocFilename;
        mIsExportNeeded = isExportNeeded;
        mKeepCssStreamOpen = keepCssStreamOpen;
    }

    public void CssSaving(CssSavingArgs args)
    {
         // 我们可以通过“Document”属性访问整个源文档。
        Assert.True(args.Document.OriginalFileName.EndsWith("Rendering.docx"));

        args.CssStream = new FileStream(mCssTextFileName, FileMode.Create);
        args.IsExportNeeded = mIsExportNeeded;
        args.KeepCssStreamOpen = mKeepCssStreamOpen;

        Assert.True(args.CssStream.CanWrite);
    }

    private readonly string mCssTextFileName;
    private readonly bool mIsExportNeeded;
    private readonly bool mKeepCssStreamOpen;
}
```

### 也可以看看

* namespace [Aspose.Words.Saving](../../aspose.words.saving)
* assembly [Aspose.Words](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Words.dll -->
