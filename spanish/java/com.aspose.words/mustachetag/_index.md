---
title: "MustacheTag"
linktitle: "MustacheTag"
second_title: "Aspose.Words para Java"
description: "Representa una etiqueta mustache en Java."
type: docs
weight: 474
url: /es/java/com.aspose.words/mustachetag/
---

**Inheritance:**
java.lang.Object
```
public class MustacheTag
```

Representa la etiqueta "mustache".
## Métodos

| Método | Descripción |
| --- | --- |
| [getReferenceOffset()](#getReferenceOffset) | Obtiene la posición inicial basada en cero de la etiqueta desde el inicio del [getReferenceRun()](../../com.aspose.words/mustachetag/\#getReferenceRun). |
| [getReferenceRun()](#getReferenceRun) | Obtiene el run que contiene el inicio de la etiqueta. |
| [getText()](#getText) | Obtiene el texto de la etiqueta. |
### getReferenceOffset() {#getReferenceOffset}
```
public int getReferenceOffset()
```


Obtiene la posición inicial basada en cero de la etiqueta desde el inicio del [getReferenceRun()](../../com.aspose.words/mustachetag/\#getReferenceRun).

**Returns:**
int - La posición inicial basada en cero de la etiqueta desde el inicio del [getReferenceRun()](../../com.aspose.words/mustachetag/\#getReferenceRun).
### getReferenceRun() {#getReferenceRun}
```
public Run getReferenceRun()
```


Obtiene el run que contiene el inicio de la etiqueta.

**Returns:**
[Run](../../com.aspose.words/run/) - The run that contains the beginning of the tag.
### getText() {#getText}
```
public String getText()
```


Obtiene el texto de la etiqueta.

**Returns:**
java.lang.String - El texto de la etiqueta.
