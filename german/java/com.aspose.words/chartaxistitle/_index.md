---
title: "ChartAxisTitle"
linktitle: "ChartAxisTitle"
second_title: "Aspose.Words für Java"
description: "Stellt Zugriff auf die Achsentitel-Eigenschaften in Java bereit."
type: docs
weight: 69
url: /de/java/com.aspose.words/chartaxistitle/
---

**Inheritance:**
java.lang.Object
```
public class ChartAxisTitle
```

Stellt Zugriff auf die Eigenschaften des Achsentitels bereit.

Weitere Informationen finden Sie im Dokumentationsartikel zu [ Working with Charts ][Working with Charts].

 **Examples:** 

Zeigt, wie man den Achsentitel eines Diagramms festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```


[Working with Charts]: https://docs.aspose.com/words/java/working-with-charts/
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getFont()](#getFont) | Stellt Zugriff auf die Schriftformatierung des Achsentitels bereit. |
| [getFormat()](#getFormat) | Stellt Zugriff auf die Füll- und Linienformatierung des Achsentitels bereit. |
| [getOrientation()](#getOrientation) | Liefert die Ausrichtung des Achsentitel-Textes. |
| [getOverlay()](#getOverlay) | Bestimmt, ob andere Diagrammelemente den Titel überlappen dürfen. |
| [getRotation()](#getRotation) | Liefert die Drehung des Achsentitels in Grad. |
| [getShow()](#getShow) | Bestimmt, ob der Titel für die Achse angezeigt werden soll. |
| [getText()](#getText) | Liefert den Text des Achsentitels. |
| [setOrientation(int value)](#setOrientation-int) | Setzt die Ausrichtung des Achsentitel-Textes. |
| [setOverlay(boolean value)](#setOverlay-boolean) | Bestimmt, ob andere Diagrammelemente den Titel überlappen dürfen. |
| [setRotation(int value)](#setRotation-int) | Setzt die Drehung des Achsentitels in Grad. |
| [setShow(boolean value)](#setShow-boolean) | Bestimmt, ob der Titel für die Achse angezeigt werden soll. |
| [setText(String value)](#setText-java.lang.String) | Setzt den Text des Achsentitels. |
### getFont() {#getFont}
```
public Font getFont()
```


Stellt Zugriff auf die Schriftformatierung des Achsentitels bereit.

 **Examples:** 

Zeigt, wie man den Achsentitel eines Diagramms festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Returns:**
[Font](../../com.aspose.words/font/) - The corresponding [Font](../../com.aspose.words/font/) value.
### getFormat() {#getFormat}
```
public ChartFormat getFormat()
```


Stellt Zugriff auf die Füll- und Linienformatierung des Achsentitels bereit.

 **Examples:** 

Zeigt, wie die Diagrammformatierung verwendet wird.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);
 Chart chart = shape.getChart();

 // Delete series generated by default.
 ChartSeriesCollection series = chart.getSeries();
 series.clear();

 String[] categories = new String[] { "Category 1", "Category 2" };
 series.add("Series 1", categories, new double[] { 1.0, 2.0 });
 series.add("Series 2", categories, new double[] { 3.0, 4.0 });

 // Format chart background.
 chart.getFormat().getFill().solid(Color.darkGray);

 // Hide axis tick labels.
 chart.getAxisX().getTickLabels().setPosition(AxisTickLabelPosition.NONE);
 chart.getAxisY().getTickLabels().setPosition(AxisTickLabelPosition.NONE);

 // Format chart title.
 chart.getTitle().getFormat().getFill().solid(Color.yellow);

 // Format axis title.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().getFormat().getFill().solid(Color.yellow);

 // Format legend.
 chart.getLegend().getFormat().getFill().solid(Color.yellow);

 doc.save(getArtifactsDir() + "Charts.ChartFormat.docx");
 
```

**Returns:**
[ChartFormat](../../com.aspose.words/chartformat/) - The corresponding [ChartFormat](../../com.aspose.words/chartformat/) value.
### getOrientation() {#getOrientation}
```
public int getOrientation()
```


Liefert die Ausrichtung des Achsentitel-Textes.

 **Remarks:** 

Der Standardwert ist [ShapeTextOrientation.HORIZONTAL](../../com.aspose.words/shapetextorientation/\#HORIZONTAL).

 **Examples:** 

Zeigt, wie man die Ausrichtung und Drehung von Diagramm- und Achsentiteln festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);
 Shape chartShape = builder.insertChart(ChartType.COLUMN, 400.0, 300.0);
 Chart chart = chartShape.getChart();

 chart.getTitle().setText("Sample Chart");
 chart.getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getTitle().setRotation(90);

 // Before setting title properties, make sure that this title will be displayed.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().setText("X Axis");
 chart.getAxisX().getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getAxisX().getTitle().setRotation(-90);

 doc.save(getArtifactsDir() + "Charts.TitleOrientation.docx");
 
```

**Returns:**
int - Die Ausrichtung des Achsentitel-Textes. Der zurückgegebene Wert ist einer der Konstanten von [ShapeTextOrientation](../../com.aspose.words/shapetextorientation/).
### getOverlay() {#getOverlay}
```
public boolean getOverlay()
```


Bestimmt, ob andere Diagrammelemente den Titel überlappen dürfen. Der Standardwert ist  false .

 **Examples:** 

Zeigt, wie man den Achsentitel eines Diagramms festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Returns:**
boolean - Der entsprechende  boolean  Wert.
### getRotation() {#getRotation}
```
public int getRotation()
```


Liefert die Drehung des Achsentitels in Grad.

 **Remarks:** 

Der zulässige Wertebereich liegt von -180 bis 180 einschließlich.

 **Examples:** 

Zeigt, wie man die Ausrichtung und Drehung von Diagramm- und Achsentiteln festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);
 Shape chartShape = builder.insertChart(ChartType.COLUMN, 400.0, 300.0);
 Chart chart = chartShape.getChart();

 chart.getTitle().setText("Sample Chart");
 chart.getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getTitle().setRotation(90);

 // Before setting title properties, make sure that this title will be displayed.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().setText("X Axis");
 chart.getAxisX().getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getAxisX().getTitle().setRotation(-90);

 doc.save(getArtifactsDir() + "Charts.TitleOrientation.docx");
 
```

**Returns:**
int - Die Drehung des Achsentitels in Grad.
### getShow() {#getShow}
```
public boolean getShow()
```


Bestimmt, ob der Titel für die Achse angezeigt werden soll. Der Standardwert ist  false .

 **Examples:** 

Zeigt, wie man den Achsentitel eines Diagramms festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Returns:**
boolean - Der entsprechende  boolean  Wert.
### getText() {#getText}
```
public String getText()
```


Liest den Text des Achsentitels. Wenn  null  oder ein leerer Wert angegeben wird, wird ein automatisch generierter Titel angezeigt.

 **Remarks:** 

Verwenden Sie die Option [getShow()](../../com.aspose.words/chartaxistitle/\#getShow) / [setShow(boolean)](../../com.aspose.words/chartaxistitle/\#setShow-boolean), wenn Sie den Titel anzeigen müssen.

 **Examples:** 

Zeigt, wie man den Achsentitel eines Diagramms festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Returns:**
java.lang.String - Der Text des Achsentitels.
### setOrientation(int value) {#setOrientation-int}
```
public void setOrientation(int value)
```


Setzt die Ausrichtung des Achsentitel-Textes.

 **Remarks:** 

Der Standardwert ist [ShapeTextOrientation.HORIZONTAL](../../com.aspose.words/shapetextorientation/\#HORIZONTAL).

 **Examples:** 

Zeigt, wie man die Ausrichtung und Drehung von Diagramm- und Achsentiteln festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);
 Shape chartShape = builder.insertChart(ChartType.COLUMN, 400.0, 300.0);
 Chart chart = chartShape.getChart();

 chart.getTitle().setText("Sample Chart");
 chart.getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getTitle().setRotation(90);

 // Before setting title properties, make sure that this title will be displayed.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().setText("X Axis");
 chart.getAxisX().getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getAxisX().getTitle().setRotation(-90);

 doc.save(getArtifactsDir() + "Charts.TitleOrientation.docx");
 
```

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | int | Die Ausrichtung des Achsentiteltextes. Der Wert muss einer der Konstanten von [ShapeTextOrientation](../../com.aspose.words/shapetextorientation/) sein. |

### setOverlay(boolean value) {#setOverlay-boolean}
```
public void setOverlay(boolean value)
```


Bestimmt, ob andere Diagrammelemente den Titel überlappen dürfen. Der Standardwert ist  false .

 **Examples:** 

Zeigt, wie man den Achsentitel eines Diagramms festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | Der entsprechende  boolean  Wert. |

### setRotation(int value) {#setRotation-int}
```
public void setRotation(int value)
```


Setzt die Drehung des Achsentitels in Grad.

 **Remarks:** 

Der zulässige Wertebereich liegt von -180 bis 180 einschließlich.

 **Examples:** 

Zeigt, wie man die Ausrichtung und Drehung von Diagramm- und Achsentiteln festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);
 Shape chartShape = builder.insertChart(ChartType.COLUMN, 400.0, 300.0);
 Chart chart = chartShape.getChart();

 chart.getTitle().setText("Sample Chart");
 chart.getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getTitle().setRotation(90);

 // Before setting title properties, make sure that this title will be displayed.
 chart.getAxisX().getTitle().setShow(true);
 chart.getAxisX().getTitle().setText("X Axis");
 chart.getAxisX().getTitle().setOrientation(ShapeTextOrientation.HORIZONTAL);
 chart.getAxisX().getTitle().setRotation(-90);

 doc.save(getArtifactsDir() + "Charts.TitleOrientation.docx");
 
```

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | Die Drehung des Achsentitels in Grad. |

### setShow(boolean value) {#setShow-boolean}
```
public void setShow(boolean value)
```


Bestimmt, ob der Titel für die Achse angezeigt werden soll. Der Standardwert ist  false .

 **Examples:** 

Zeigt, wie man den Achsentitel eines Diagramms festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | Der entsprechende  boolean  Wert. |

### setText(String value) {#setText-java.lang.String}
```
public void setText(String value)
```


Setzt den Text des Achsentitels. Wenn  null  oder ein leerer Wert angegeben wird, wird ein automatisch generierter Titel angezeigt.

 **Remarks:** 

Verwenden Sie die Option [getShow()](../../com.aspose.words/chartaxistitle/\#getShow) / [setShow(boolean)](../../com.aspose.words/chartaxistitle/\#setShow-boolean), wenn Sie den Titel anzeigen müssen.

 **Examples:** 

Zeigt, wie man den Achsentitel eines Diagramms festlegt.

```

 Document doc = new Document();
 DocumentBuilder builder = new DocumentBuilder(doc);

 Shape shape = builder.insertChart(ChartType.COLUMN, 432.0, 252.0);

 Chart chart = shape.getChart();
 ChartSeriesCollection seriesColl = chart.getSeries();
 // Delete default generated series.
 seriesColl.clear();

 seriesColl.add("AW Series 1", new String[] { "AW Category 1", "AW Category 2" }, new double[] { 1.0, 2.0 });

 // Set axis title.
 ChartAxisTitle chartAxisXTitle = chart.getAxisX().getTitle();
 chartAxisXTitle.setText("Categories");
 chartAxisXTitle.setShow(true);
 ChartAxisTitle chartAxisYTitle = chart.getAxisY().getTitle();
 chartAxisYTitle.setText("Values");
 chartAxisYTitle.setShow(true);
 chartAxisYTitle.setOverlay(true);
 chartAxisYTitle.getFont().setSize(12.0);
 chartAxisYTitle.getFont().setColor(Color.BLUE);

 doc.save(getArtifactsDir() + "Charts.ChartAxisTitle.docx");
 
```

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | java.lang.String | Der Text des Achsentitels. |

